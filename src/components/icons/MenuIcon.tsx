import { AiOutlineMenu } from 'react-icons/ai';

import type { IconBaseProps } from 'react-icons';

export default function MenuIcon(props: IconBaseProps) {
  return <AiOutlineMenu aria-hidden="true" {...props} />;
}
