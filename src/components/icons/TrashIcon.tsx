import { AiOutlineDelete } from 'react-icons/ai';

import type { IconBaseProps } from 'react-icons';

export default function TrashIcon(props: IconBaseProps) {
  return <AiOutlineDelete aria-hidden="true" {...props} />;
}
