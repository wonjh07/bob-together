import { AiOutlineHome } from 'react-icons/ai';

import type { IconBaseProps } from 'react-icons';

export default function HomeIcon(props: IconBaseProps) {
  return <AiOutlineHome aria-hidden="true" {...props} />;
}
