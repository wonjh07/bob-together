import { AiOutlineEdit } from 'react-icons/ai';

import type { IconBaseProps } from 'react-icons';

export default function ReviewIcon(props: IconBaseProps) {
  return <AiOutlineEdit aria-hidden="true" {...props} />;
}
