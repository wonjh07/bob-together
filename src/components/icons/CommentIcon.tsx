import { AiOutlineComment } from 'react-icons/ai';

import type { IconBaseProps } from 'react-icons';

export default function CommentIcon(props: IconBaseProps) {
  return <AiOutlineComment aria-hidden="true" {...props} />;
}
