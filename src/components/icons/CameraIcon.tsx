import { AiOutlineCamera } from 'react-icons/ai';

import type { IconBaseProps } from 'react-icons';

export default function CameraIcon(props: IconBaseProps) {
  return <AiOutlineCamera aria-hidden="true" {...props} />;
}
