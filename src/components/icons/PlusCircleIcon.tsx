import { AiOutlinePlusCircle } from 'react-icons/ai';

import type { IconBaseProps } from 'react-icons';

export default function PlusCircleIcon(props: IconBaseProps) {
  return <AiOutlinePlusCircle aria-hidden="true" {...props} />;
}
