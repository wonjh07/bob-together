import { AiOutlineClockCircle } from 'react-icons/ai';

import type { IconBaseProps } from 'react-icons';

export default function ClockIcon(props: IconBaseProps) {
  return <AiOutlineClockCircle aria-hidden="true" {...props} />;
}
