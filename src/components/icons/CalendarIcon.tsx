import { AiOutlineCalendar } from 'react-icons/ai';

import type { IconBaseProps } from 'react-icons';

export default function CalendarIcon(props: IconBaseProps) {
  return <AiOutlineCalendar aria-hidden="true" {...props} />;
}
