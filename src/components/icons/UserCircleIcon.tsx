import { AiOutlineUser } from 'react-icons/ai';

import type { IconBaseProps } from 'react-icons';

export default function UserCircleIcon(props: IconBaseProps) {
  return <AiOutlineUser aria-hidden="true" {...props} />;
}
